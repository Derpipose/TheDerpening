apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: $NAMESPACE
  name: thederpening-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: ${PULL_REQUEST_ID}.derpserver.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: thederpening-service
            port:
              number: 80

              